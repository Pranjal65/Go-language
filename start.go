package main 

import "fmt"


// main function is considered as the entry point for the code or programm which should be in only one file in files with same package
func start_func(){
      sayHellofunc("Pranjal");
	  for _ ,v:= range qr{
		 fmt.Println(v);
	  }
}